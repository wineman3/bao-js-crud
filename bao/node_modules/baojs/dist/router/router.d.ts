import { IHandler } from "../bao";
import Node from "./tree";
export default class BaoRouter {
    trees: {
        [key: string]: Node;
    };
    opts: {
        [key: string]: any;
    };
    constructor(opts?: {});
    any(path: string, handler: IHandler): void;
    on(method: string, path: string, handler: IHandler): this;
    find(method: any, path: any): IRouterResponse;
}
export interface IRouterResponse {
    handler: IHandler | null;
    params: {
        [key: string]: string;
    };
}
